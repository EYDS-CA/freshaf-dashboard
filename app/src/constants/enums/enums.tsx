export enum Emoji {
  Gold = '🥇',
  Silver = '🥈',
  Bronze = '🥉',
  Pop = '💩',
  Fire = '🔥',
  Trophy = '🏆',
  Check = '✅',
}

export enum MedalText {
  Gold = 'Gold',
  Silver = 'Silver',
  Bronze = 'Bronze',
  Pop = 'N/A',
}

export enum AppraisalTitles {
  ProjectLevel = 'Project Level',
  TotalScore = 'Total Score',
  Ranking = 'Ranking',
  TotalMedals = 'Total Medals',
}

export enum LeaderBoardTabs {
  Project = 'Project',
  Developer = 'Developer',
}
